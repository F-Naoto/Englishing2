<%= form_with(model: current_student.active_relationships.build, local: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @teacher.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
