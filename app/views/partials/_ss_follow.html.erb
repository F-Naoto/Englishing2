<% if current_student %>
  <%= form_with(model: current_student.ss_active_relationships.build, local: true) do |f| %>
    <div><%= hidden_field_tag :followed_id, student.id %></div>
    <%= f.submit "Follow", class: "btn btn-primary" %>
  <% end %>
<% end %>
