follow_form = document.querySelector("#follow_form")
followers = document.querySelector("#followers")
follow_form.outerHTML = "<%= j(render('partials/ss_follow', student: @student)) %>"
followers.innerHTML = "<%= @followed_student.followers.count %>"
