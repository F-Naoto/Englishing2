<% if current_student %>
  <%= form_with(model: current_student.st_active_relationships.build, local: false) do |f| %>
    <%= hidden_field_tag :followed_id, teacher.id %>
    <%= f.submit "Follow", class: "bg-white" %>
  <% end %>
<% end %>
