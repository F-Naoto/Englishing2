<%= form_with(model: current_student.ss_active_relationships.build, local: false, id: "follow") do |f| %>
  <%= hidden_field_tag :followed_id, student.id %>
  <%= f.submit "Follow", class:"bg-white font-bold" %>
<% end %>
